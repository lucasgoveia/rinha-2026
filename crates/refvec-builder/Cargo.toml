[package]
name = "refvec-builder"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "refvec-builder"
path = "src/main.rs"

[dependencies]
ivf-core = { path = "../ivf-core" }
clap = { version = "4", features = ["derive"] }
